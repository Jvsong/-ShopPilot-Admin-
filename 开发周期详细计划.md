# 电商后台管理系统 - 开发周期详细计划

## 项目概述

### 项目基本信息
- **项目名称**：电商后台管理系统（自动化测试验证目标）
- **开发周期**：6周（2026年4月4日 - 2026年5月15日）
- **技术架构**：Spring Boot 2.7 + Vue 3 + MySQL 8.0
- **项目负责人**：宋佳伟
- **指导教师**：张刘飘 讲师
- **对接项目**：电商后台自动化测试框架（f:\Desk\graduation project\ecommerce-autotest）

### 项目目标
1. 提供完整的电商后台管理功能，支持自动化测试验证
2. 实现前后端分离架构，确保与自动化测试框架页面元素完全兼容
3. 完成五大核心模块（登录、商品管理、订单管理、用户管理、权限管理）
4. 支持多环境部署，提供完整的测试数据初始化
5. 生成详细的API文档和部署指南

### 开发原则
1. **兼容性优先**：页面元素ID、class必须与自动化测试框架完全匹配
2. **前后端分离**：清晰的API契约，并行开发
3. **模块化设计**：各功能模块独立开发，便于维护和扩展
4. **安全可靠**：遵循安全最佳实践，防护常见安全漏洞
5. **可测试性**：提供完整的测试数据管理和健康检查接口

### 与自动化测试框架的兼容性要求
**强制要求**：以下页面元素ID必须严格匹配测试框架定义：
- 登录页面：`username`, `password`, `login-btn`, `remember-me`, `error-msg`
- 商品管理：`product-search`, `product-table`, `add-product-btn`, `product-name`, `product-price`
- 订单管理：`order-search-input`, `status-filter`, `order-table`
- 用户管理：`user-search-input`, `user-table`

## 环境准备阶段（第0周：2026年3月28日-4月3日）

### 目标
完成开发环境搭建，熟悉技术栈，准备开发工具，理解自动化测试框架需求。

### 具体任务
1. **开发环境配置**
   - [ ] 安装JDK 11+（推荐Amazon Corretto 11）
   - [ ] 安装Node.js 16+和npm/yarn
   - [ ] 安装MySQL 8.0+和MySQL Workbench
   - [ ] 安装IntelliJ IDEA Ultimate / VS Code
   - [ ] 安装Git并进行基础配置
   - [ ] 安装Docker和Docker Compose（可选）

2. **技术栈学习**
   - [ ] 学习Spring Boot基础架构和常用注解
   - [ ] 掌握Vue 3 Composition API和Vite构建工具
   - [ ] 了解Element Plus组件库使用方法
   - [ ] 学习MyBatis-Plus数据访问层
   - [ ] 掌握JWT认证和Spring Security集成

3. **自动化测试框架研究**
   - [ ] 详细阅读自动化测试框架的需求文档和架构计划
   - [ ] 运行自动化测试框架，理解测试流程和页面元素要求
   - [ ] 分析测试框架的页面对象模型（POM）定位器定义
   - [ ] 制定兼容性保障策略

4. **项目规划**
   - [ ] 详细阅读本项目的需求文档和计划架构文档
   - [ ] 制定个人开发时间表
   - [ ] 准备开发笔记本和问题记录本
   - [ ] 创建项目代码仓库（GitHub/GitLab）

### 交付物
- 配置完成的开发环境
- 技术学习笔记和参考资料
- 自动化测试框架分析报告
- 个人开发计划表

## 第一阶段：基础框架搭建（第1周：2026年4月4日-4月10日）

### 第1周目标
完成项目初始化，搭建可运行的基础框架，确保与自动化测试框架的登录页面兼容性。

### 第1周详细任务

#### 周一（4月4日）：项目初始化和目录结构创建
1. **创建项目根目录结构**
   ```bash
   cd F:\Desk\shop-system
   mkdir -p backend frontend database docker docs
   ```

2. **初始化后端Spring Boot项目**
   ```bash
   # 使用Spring Initializr生成项目
   # 访问 https://start.spring.io/
   # 选择: Spring Boot 2.7.x, Java 11, Packaging: Jar
   # 依赖: Spring Web, Spring Security, MyBatis, MySQL, Validation, Lombok
   ```

3. **初始化前端Vue项目**
   ```bash
   cd frontend
   npm create vue@latest
   # 项目名称: shop-system-frontend
   # 选择: TypeScript, Vue Router, Pinia, ESLint, Prettier
   ```

4. **创建基础配置文件**
   ```bash
   # 后端配置文件
   touch backend/src/main/resources/application.yml
   touch backend/src/main/resources/application-dev.yml
   
   # 前端配置文件
   touch frontend/.env.development
   touch frontend/.env.production
   
   # 数据库脚本
   touch database/init.sql
   touch database/test_data.sql
   ```

5. **初始化Git仓库**
   ```bash
   cd F:\Desk\shop-system
   git init
   echo ".idea/" > .gitignore
   echo "target/" >> .gitignore
   echo "node_modules/" >> .gitignore
   echo "dist/" >> .gitignore
   echo "logs/" >> .gitignore
   echo "*.iml" >> .gitignore
   ```

#### 周二（4月5日）：数据库设计和基础实体类
1. **设计核心数据库表结构**
   ```sql
   -- database/init.sql
   -- 用户表
   CREATE TABLE `user` (
     `id` bigint NOT NULL AUTO_INCREMENT COMMENT '用户ID',
     `username` varchar(50) NOT NULL COMMENT '用户名',
     `password` varchar(255) NOT NULL COMMENT '密码',
     `email` varchar(100) DEFAULT NULL COMMENT '邮箱',
     `phone` varchar(20) DEFAULT NULL COMMENT '手机号',
     `avatar` varchar(255) DEFAULT NULL COMMENT '头像',
     `status` tinyint DEFAULT '1' COMMENT '状态: 0-禁用, 1-正常',
     `user_type` tinyint DEFAULT '1' COMMENT '用户类型: 1-普通用户, 2-管理员',
     `login_fail_count` int DEFAULT '0' COMMENT '登录失败次数',
     `lock_time` datetime DEFAULT NULL COMMENT '锁定时间',
     `last_login_time` datetime DEFAULT NULL COMMENT '最后登录时间',
     `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
     `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
     PRIMARY KEY (`id`),
     UNIQUE KEY `uk_username` (`username`),
     UNIQUE KEY `uk_email` (`email`),
     KEY `idx_status` (`status`)
   ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
   ```

2. **创建实体类**
   ```java
   // backend/src/main/java/com/shop/dao/entity/User.java
   package com.shop.dao.entity;
   
   import com.baomidou.mybatisplus.annotation.*;
   import lombok.Data;
   import java.time.LocalDateTime;
   
   @Data
   @TableName("user")
   public class User {
       @TableId(type = IdType.AUTO)
       private Long id;
       
       private String username;
       private String password;
       private String email;
       private String phone;
       private String avatar;
       private Integer status;
       private Integer userType;
       private Integer loginFailCount;
       private LocalDateTime lockTime;
       private LocalDateTime lastLoginTime;
       
       @TableField(fill = FieldFill.INSERT)
       private LocalDateTime createTime;
       
       @TableField(fill = FieldFill.INSERT_UPDATE)
       private LocalDateTime updateTime;
   }
   ```

3. **创建MyBatis Mapper接口**
   ```java
   // backend/src/main/java/com/shop/dao/mapper/UserMapper.java
   package com.shop.dao.mapper;
   
   import com.baomidou.mybatisplus.core.mapper.BaseMapper;
   import com.shop.dao.entity.User;
   import org.apache.ibatis.annotations.Mapper;
   
   @Mapper
   public interface UserMapper extends BaseMapper<User> {
   }
   ```

4. **编写数据库初始化脚本**
   ```sql
   -- database/test_data.sql
   -- 初始化管理员账号（必须与自动化测试框架数据一致）
   INSERT INTO `user` (username, password, email, user_type, status) 
   VALUES ('admin', '$2a$10$YourBcryptHashHere', 'admin@shop.com', 2, 1);
   ```

#### 周三（4月6日）：Spring Boot基础框架配置
1. **配置应用主配置文件**
   ```yaml
   # backend/src/main/resources/application.yml
   server:
     port: 8080
     servlet:
       context-path: /
   
   spring:
     application:
       name: shop-system
     profiles:
       active: dev
     datasource:
       driver-class-name: com.mysql.cj.jdbc.Driver
       url: jdbc:mysql://localhost:3306/shop_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
       username: root
       password: root123
       hikari:
         maximum-pool-size: 20
         minimum-idle: 5
   
     jackson:
       date-format: yyyy-MM-dd HH:mm:ss
       time-zone: Asia/Shanghai
   
   mybatis-plus:
     configuration:
       map-underscore-to-camel-case: true
       log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
     global-config:
       db-config:
         logic-delete-field: isDeleted
         logic-delete-value: 1
         logic-not-delete-value: 0
   ```

2. **配置开发环境配置文件**
   ```yaml
   # backend/src/main/resources/application-dev.yml
   spring:
     datasource:
       url: jdbc:mysql://localhost:3306/shop_system_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
   
   logging:
     level:
       com.shop: DEBUG
       org.springframework.web: DEBUG
   ```

3. **创建统一响应格式**
   ```java
   // backend/src/main/java/com/shop/dto/response/ApiResponse.java
   package com.shop.dto.response;
   
   import lombok.Data;
   import java.time.LocalDateTime;
   
   @Data
   public class ApiResponse<T> {
       private Integer code;
       private String message;
       private T data;
       private LocalDateTime timestamp;
       
       public static <T> ApiResponse<T> success(T data) {
           ApiResponse<T> response = new ApiResponse<>();
           response.setCode(200);
           response.setMessage("success");
           response.setData(data);
           response.setTimestamp(LocalDateTime.now());
           return response;
       }
       
       public static ApiResponse<?> error(Integer code, String message) {
           ApiResponse<?> response = new ApiResponse<>();
           response.setCode(code);
           response.setMessage(message);
           response.setTimestamp(LocalDateTime.now());
           return response;
       }
   }
   ```

4. **创建全局异常处理器**
   ```java
   // backend/src/main/java/com/shop/exception/GlobalExceptionHandler.java
   package com.shop.exception;
   
   import com.shop.dto.response.ApiResponse;
   import lombok.extern.slf4j.Slf4j;
   import org.springframework.web.bind.annotation.ExceptionHandler;
   import org.springframework.web.bind.annotation.RestControllerAdvice;
   
   @Slf4j
   @RestControllerAdvice
   public class GlobalExceptionHandler {
       
       @ExceptionHandler(Exception.class)
       public ApiResponse<?> handleException(Exception e) {
           log.error("系统异常: ", e);
           return ApiResponse.error(500, "系统内部错误");
       }
       
       @ExceptionHandler(BusinessException.class)
       public ApiResponse<?> handleBusinessException(BusinessException e) {
           log.warn("业务异常: {}", e.getMessage());
           return ApiResponse.error(e.getCode(), e.getMessage());
       }
   }
   ```

#### 周四（4月7日）：Vue前端基础框架配置
1. **配置Vite开发环境**
   ```javascript
   // frontend/vite.config.ts
   import { defineConfig } from 'vite'
   import vue from '@vitejs/plugin-vue'
   import { resolve } from 'path'
   
   export default defineConfig({
     plugins: [vue()],
     resolve: {
       alias: {
         '@': resolve(__dirname, 'src')
       }
     },
     server: {
       port: 3000,
       proxy: {
         '/api': {
           target: 'http://localhost:8080',
           changeOrigin: true
         }
       }
     }
   })
   ```

2. **配置Element Plus组件库**
   ```typescript
   // frontend/src/plugins/element-plus.ts
   import ElementPlus from 'element-plus'
   import 'element-plus/dist/index.css'
   import zhCn from 'element-plus/es/locale/lang/zh-cn'
   
   export default {
     install(app: any) {
       app.use(ElementPlus, {
         locale: zhCn,
         size: 'default'
       })
     }
   }
   ```

3. **配置Axios请求封装**
   ```typescript
   // frontend/src/utils/request.ts
   import axios from 'axios'
   import { ElMessage } from 'element-plus'
   import type { InternalAxiosRequestConfig, AxiosResponse } from 'axios'
   
   const request = axios.create({
     baseURL: import.meta.env.VITE_API_BASE_URL || '/api',
     timeout: 10000
   })
   
   // 请求拦截器
   request.interceptors.request.use(
     (config: InternalAxiosRequestConfig) => {
       const token = localStorage.getItem('token')
       if (token) {
         config.headers.Authorization = `Bearer ${token}`
       }
       return config
     },
     (error) => {
       return Promise.reject(error)
     }
   )
   
   // 响应拦截器
   request.interceptors.response.use(
     (response: AxiosResponse) => {
       const { code, message, data } = response.data
       if (code === 200) {
         return data
       } else {
         ElMessage.error(message || '请求失败')
         return Promise.reject(new Error(message))
       }
     },
     (error) => {
       ElMessage.error(error.message || '请求失败')
       return Promise.reject(error)
     }
   )
   
   export default request
   ```

4. **配置路由和状态管理**
   ```typescript
   // frontend/src/router/index.ts
   import { createRouter, createWebHistory } from 'vue-router'
   import type { RouteRecordRaw } from 'vue-router'
   
   const routes: RouteRecordRaw[] = [
     {
       path: '/admin',
       name: 'Admin',
       redirect: '/admin/login',
       children: [
         {
           path: 'login',
           name: 'Login',
           component: () => import('@/views/admin/Login.vue')
         }
       ]
     }
   ]
   
   const router = createRouter({
     history: createWebHistory(),
     routes
   })
   
   export default router
   ```

#### 周五（4月8日）：登录页面开发（兼容性关键）
1. **创建登录页面组件（必须严格匹配自动化测试框架）**
   ```vue
   <!-- frontend/src/views/admin/Login.vue -->
   <template>
     <div class="login-container">
       <div class="login-box">
         <h2>电商后台管理系统</h2>
         <form @submit.prevent="handleLogin">
           <div class="form-group">
             <input
               type="text"
               id="username"                <!-- 必须：自动化测试定位器 -->
               v-model="form.username"
               placeholder="用户名"
               required
               autocomplete="username"
             />
           </div>
           <div class="form-group">
             <input
               type="password"
               id="password"                <!-- 必须：自动化测试定位器 -->
               v-model="form.password"
               placeholder="密码"
               required
               autocomplete="current-password"
             />
           </div>
           <div class="form-group remember-me">
             <label>
               <input
                 type="checkbox"
                 id="remember-me"            <!-- 必须：自动化测试定位器 -->
                 v-model="form.rememberMe"
               />
               记住我
             </label>
             <a href="/forgot-password" id="forgot-password">忘记密码</a>
           </div>
           <button
             type="submit"
             id="login-btn"                  <!-- 必须：自动化测试定位器 -->
             :disabled="loading"
             class="login-button"
           >
             {{ loading ? '登录中...' : '登录' }}
           </button>
           <div
             v-if="errorMessage"
             class="error-msg"                <!-- 必须：自动化测试定位器 -->
           >
             {{ errorMessage }}
           </div>
         </form>
       </div>
     </div>
   </template>
   
   <script setup lang="ts">
   import { ref, reactive } from 'vue'
   import { useRouter } from 'vue-router'
   import { ElMessage } from 'element-plus'
   import request from '@/utils/request'
   
   const router = useRouter()
   const loading = ref(false)
   const errorMessage = ref('')
   
   const form = reactive({
     username: '',
     password: '',
     rememberMe: false
   })
   
   const handleLogin = async () => {
     if (!form.username || !form.password) {
       errorMessage.value = '用户名和密码不能为空'
       return
     }
     
     loading.value = true
     errorMessage.value = ''
     
     try {
       const response = await request.post('/auth/login', {
         username: form.username,
         password: form.password,
         rememberMe: form.rememberMe
       })
       
       // 存储token
       localStorage.setItem('token', response.token)
       if (form.rememberMe) {
         localStorage.setItem('rememberMe', 'true')
       }
       
       ElMessage.success('登录成功')
       router.push('/admin/dashboard')
     } catch (error: any) {
       errorMessage.value = error.message || '登录失败，请检查用户名和密码'
     } finally {
       loading.value = false
     }
   }
   </script>
   
   <style scoped>
   .login-container {
     display: flex;
     justify-content: center;
     align-items: center;
     height: 100vh;
     background-color: #f5f5f5;
   }
   
   .login-box {
     width: 400px;
     padding: 40px;
     background: white;
     border-radius: 8px;
     box-shadow: 0 2px 12px rgba(0, 0, 0, 0.1);
   }
   
   .form-group {
     margin-bottom: 20px;
   }
   
   input[type="text"],
   input[type="password"] {
     width: 100%;
     padding: 12px;
     border: 1px solid #dcdfe6;
     border-radius: 4px;
     font-size: 14px;
   }
   
   .remember-me {
     display: flex;
     justify-content: space-between;
     align-items: center;
   }
   
   .login-button {
     width: 100%;
     padding: 12px;
     background-color: #409eff;
     color: white;
     border: none;
     border-radius: 4px;
     font-size: 16px;
     cursor: pointer;
   }
   
   .login-button:disabled {
     background-color: #a0cfff;
     cursor: not-allowed;
   }
   
   .error-msg {
     margin-top: 15px;
     padding: 10px;
     background-color: #fef0f0;
     color: #f56c6c;
     border-radius: 4px;
     font-size: 14px;
   }
   </style>
   ```

2. **创建后端登录API接口**
   ```java
   // backend/src/main/java/com/shop/controller/admin/AdminAuthController.java
   package com.shop.controller.admin;
   
   import com.shop.dto.request.LoginRequest;
   import com.shop.dto.response.ApiResponse;
   import com.shop.service.AuthService;
   import org.springframework.beans.factory.annotation.Autowired;
   import org.springframework.web.bind.annotation.*;
   
   import javax.validation.Valid;
   
   @RestController
   @RequestMapping("/api/auth")
   public class AdminAuthController {
       
       @Autowired
       private AuthService authService;
       
       @PostMapping("/login")
       public ApiResponse<?> login(@Valid @RequestBody LoginRequest request) {
           String token = authService.login(request.getUsername(), request.getPassword());
           return ApiResponse.success(token);
       }
       
       @PostMapping("/logout")
       public ApiResponse<?> logout() {
           authService.logout();
           return ApiResponse.success(null);
       }
   }
   ```

#### 周六（4月9日）：前后端联调与兼容性验证
1. **启动前后端开发环境**
   ```bash
   # 启动后端
   cd backend
   mvn spring-boot:run
   
   # 启动前端
   cd frontend
   npm run dev
   ```

2. **验证登录页面可访问性**
   - 访问 http://localhost:3000/admin/login
   - 确认页面正常显示
   - 检查页面元素ID是否正确

3. **与自动化测试框架对接验证**
   ```bash
   # 在自动化测试框架项目中
   cd f:\Desk\graduation project\ecommerce-autotest
   
   # 更新配置文件中的base_url
   # config/config.yaml
   # environment.base_url: "http://localhost:8080/admin"
   
   # 运行登录页面元素检查
   python run.py --check
   
   # 运行登录测试（首次对接）
   python run.py --run login
   ```

4. **修复兼容性问题**
   - 根据测试反馈调整页面元素
   - 确保错误提示显示正确
   - 优化页面加载性能

#### 周日（4月10日）：第一阶段验收和文档
1. **第一阶段功能验收**
   - 运行前后端完整启动流程
   - 验证登录功能正常工作
   - 测试数据库连接和初始化
   - 检查页面响应时间

2. **兼容性验证报告**
   - 记录自动化测试框架对接结果
   - 列出发现的问题和解决方案
   - 确认页面元素匹配完整性

3. **编写第一阶段文档**
   - 更新README.md项目说明
   - 编写环境搭建文档
   - 创建API接口文档（初步）
   - 记录技术难点和解决方案

### 第1周交付物
- [x] 完整的项目目录结构
- [x] 数据库设计和初始化脚本
- [x] Spring Boot基础框架
- [x] Vue 3前端基础框架
- [x] 登录页面（兼容自动化测试框架）
- [x] 前后端联调环境
- [x] 第一阶段验收报告
- [x] 兼容性验证记录

## 第二阶段：核心业务模块开发（第2-4周：2026年4月11日-5月1日）

### 第2周目标（2026年4月11日-4月17日）
完成用户认证和权限管理模块，确保与自动化测试框架的用户管理测试用例兼容。

### 第2周详细任务

#### 周一（4月11日）：用户管理后端开发
1. **用户管理API设计**
   ```java
   // backend/src/main/java/com/shop/controller/admin/AdminUserController.java
   @RestController
   @RequestMapping("/api/admin/users")
   public class AdminUserController {
       
       @GetMapping("")
       public ApiResponse<?> listUsers(
           @RequestParam(defaultValue = "1") Integer page,
           @RequestParam(defaultValue = "10") Integer size,
           @RequestParam(required = false) String username,
           @RequestParam(required = false) String email,
           @RequestParam(required = false) Integer status
       ) {
           // 用户列表查询
       }
       
       @GetMapping("/{id}")
       public ApiResponse<?> getUserDetail(@PathVariable Long id) {
           // 用户详情
       }
       
       @PostMapping("")
       public ApiResponse<?> createUser(@Valid @RequestBody UserCreateRequest request) {
           // 创建用户
       }
       
       @PutMapping("/{id}")
       public ApiResponse<?> updateUser(@PathVariable Long id, @Valid @RequestBody UserUpdateRequest request) {
           // 更新用户
       }
       
       @PatchMapping("/{id}/status")
       public ApiResponse<?> updateUserStatus(@PathVariable Long id, @RequestParam Integer status) {
           // 更新用户状态
       }
       
       @DeleteMapping("/{id}")
       public ApiResponse<?> deleteUser(@PathVariable Long id) {
           // 删除用户（软删除）
       }
   }
   ```

2. **用户服务层实现**
   ```java
   // backend/src/main/java/com/shop/service/impl/UserServiceImpl.java
   @Service
   public class UserServiceImpl implements UserService {
       
       @Autowired
       private UserMapper userMapper;
       
       @Override
       public Page<User> listUsers(UserQuery query) {
           Page<User> page = new Page<>(query.getPage(), query.getSize());
           LambdaQueryWrapper<User> wrapper = new LambdaQueryWrapper<>();
           
           if (StringUtils.isNotBlank(query.getUsername())) {
               wrapper.like(User::getUsername, query.getUsername());
           }
           
           if (StringUtils.isNotBlank(query.getEmail())) {
               wrapper.like(User::getEmail, query.getEmail());
           }
           
           if (query.getStatus() != null) {
               wrapper.eq(User::getStatus, query.getStatus());
           }
           
           wrapper.orderByDesc(User::getCreateTime);
           return userMapper.selectPage(page, wrapper);
       }
   }
   ```

#### 周二（4月12日）：用户管理前端开发
1. **用户列表页面（必须兼容自动化测试框架）**
   ```vue
   <!-- frontend/src/views/admin/User/List.vue -->
   <template>
     <div class="user-management">
       <div class="search-bar">
         <el-input
           v-model="searchParams.username"
           placeholder="搜索用户名"
           id="user-search-input"  <!-- 必须：自动化测试定位器 -->
           style="width: 200px; margin-right: 10px;"
         />
         <el-select
           v-model="searchParams.status"
           placeholder="状态"
           clearable
           style="width: 120px; margin-right: 10px;"
         >
           <el-option label="正常" value="1" />
           <el-option label="禁用" value="0" />
         </el-select>
         <el-button type="primary" @click="handleSearch">搜索</el-button>
         <el-button @click="handleReset">重置</el-button>
       </div>
       
       <el-table
         :data="userList"
         v-loading="loading"
         id="user-table"  <!-- 必须：自动化测试定位器 -->
         style="width: 100%; margin-top: 20px;"
       >
         <el-table-column prop="id" label="ID" width="80" />
         <el-table-column prop="username" label="用户名" width="120" />
         <el-table-column prop="email" label="邮箱" width="180" />
         <el-table-column prop="phone" label="手机号" width="130" />
         <el-table-column prop="status" label="状态" width="80">
           <template #default="scope">
             <el-tag :type="scope.row.status === 1 ? 'success' : 'danger'">
               {{ scope.row.status === 1 ? '正常' : '禁用' }}
             </el-tag>
           </template>
         </el-table-column>
         <el-table-column prop="createTime" label="创建时间" width="180" />
         <el-table-column label="操作" width="200">
           <template #default="scope">
             <el-button size="small" @click="handleEdit(scope.row)">编辑</el-button>
             <el-button
               size="small"
               :type="scope.row.status === 1 ? 'danger' : 'success'"
               @click="handleToggleStatus(scope.row)"
             >
               {{ scope.row.status === 1 ? '禁用' : '启用' }}
             </el-button>
           </template>
         </el-table-column>
       </el-table>
       
       <el-pagination
         v-model:current-page="pagination.current"
         v-model:page-size="pagination.size"
         :total="pagination.total"
         @current-change="handlePageChange"
         @size-change="handleSizeChange"
         style="margin-top: 20px; text-align: right;"
       />
     </div>
   </template>
   ```

2. **用户API接口封装**
   ```typescript
   // frontend/src/api/user.ts
   import request from '@/utils/request'
   
   export interface UserQueryParams {
     page?: number
     size?: number
     username?: string
     email?: string
     status?: number
   }
   
   export interface User {
     id: number
     username: string
     email: string
     phone: string
     status: number
     createTime: string
   }
   
   export const userApi = {
     // 获取用户列表
     getList(params: UserQueryParams) {
       return request.get('/admin/users', { params })
     },
     
     // 获取用户详情
     getDetail(id: number) {
       return request.get(`/admin/users/${id}`)
     },
     
     // 创建用户
     create(data: any) {
       return request.post('/admin/users', data)
     },
     
     // 更新用户
     update(id: number, data: any) {
       return request.put(`/admin/users/${id}`, data)
     },
     
     // 更新用户状态
     updateStatus(id: number, status: number) {
       return request.patch(`/admin/users/${id}/status`, null, {
         params: { status }
       })
     },
     
     // 删除用户
     delete(id: number) {
       return request.delete(`/admin/users/${id}`)
     }
   }
   ```

#### 周三（4月13日）：权限管理模块开发
1. **角色和权限实体设计**
   ```java
   // backend/src/main/java/com/shop/dao/entity/Role.java
   @Data
   @TableName("role")
   public class Role {
       @TableId(type = IdType.AUTO)
       private Long id;
       
       private String name;
       private String code;
       private String description;
       private Integer status;
       
       @TableField(fill = FieldFill.INSERT)
       private LocalDateTime createTime;
   }
   
   // backend/src/main/java/com/shop/dao/entity/Permission.java
   @Data
   @TableName("permission")
   public class Permission {
       @TableId(type = IdType.AUTO)
       private Long id;
       
       private String name;
       private String code;
       private Integer type; // 1-菜单, 2-操作
       private Long parentId;
       private String path;
       private String icon;
       private Integer sortOrder;
       private Integer status;
       
       @TableField(fill = FieldFill.INSERT)
       private LocalDateTime createTime;
   }
   ```

2. **权限管理API接口**
   ```java
   // backend/src/main/java/com/shop/controller/admin/AdminPermissionController.java
   @RestController
   @RequestMapping("/api/admin/permissions")
   public class AdminPermissionController {
       
       @GetMapping("/roles")
       public ApiResponse<?> listRoles() {
           // 角色列表
       }
       
       @PostMapping("/roles")
       public ApiResponse<?> createRole(@Valid @RequestBody RoleCreateRequest request) {
           // 创建角色
       }
       
       @PutMapping("/roles/{id}")
       public ApiResponse<?> updateRole(@PathVariable Long id, @Valid @RequestBody RoleUpdateRequest request) {
           // 更新角色
       }
       
       @GetMapping("/menu")
       public ApiResponse<?> getMenuTree() {
           // 获取菜单树
       }
       
       @PutMapping("/roles/{id}/permissions")
       public ApiResponse<?> assignPermissions(@PathVariable Long id, @RequestBody List<Long> permissionIds) {
           // 分配权限
       }
   }
   ```

#### 周四（4月14日）：Spring Security集成
1. **JWT认证配置**
   ```java
   // backend/src/main/java/com/shop/security/JwtTokenUtil.java
   @Component
   public class JwtTokenUtil {
       
       private final String secret = "your-jwt-secret-key-change-in-production";
       private final Long expiration = 86400000L; // 24小时
       
       public String generateToken(String username) {
           Map<String, Object> claims = new HashMap<>();
           claims.put("username", username);
           claims.put("created", new Date());
           
           return Jwts.builder()
               .setClaims(claims)
               .setExpiration(new Date(System.currentTimeMillis() + expiration))
               .signWith(SignatureAlgorithm.HS512, secret)
               .compact();
       }
       
       public String getUsernameFromToken(String token) {
           Claims claims = getClaimsFromToken(token);
           return claims.get("username", String.class);
       }
       
       public boolean validateToken(String token) {
           try {
               getClaimsFromToken(token);
               return true;
           } catch (Exception e) {
               return false;
           }
       }
   }
   ```

2. **Spring Security配置类**
   ```java
   // backend/src/main/java/com/shop/config/SecurityConfig.java
   @Configuration
   @EnableWebSecurity
   public class SecurityConfig {
       
       @Autowired
       private JwtAuthenticationFilter jwtAuthenticationFilter;
       
       @Bean
       public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
           http
               .csrf().disable()
               .sessionManagement().sessionCreationPolicy(SessionCreationPolicy.STATELESS)
               .and()
               .authorizeHttpRequests()
               .antMatchers("/api/auth/**").permitAll()
               .antMatchers("/api/admin/**").hasRole("ADMIN")
               .antMatchers("/api/**").authenticated()
               .anyRequest().permitAll()
               .and()
               .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
           
           return http.build();
       }
       
       @Bean
       public PasswordEncoder passwordEncoder() {
           return new BCryptPasswordEncoder();
       }
   }
   ```

#### 周五（4月15日）：权限管理前端开发
1. **角色管理页面**
   ```vue
   <!-- frontend/src/views/admin/Permission/RoleManagement.vue -->
   <template>
     <div class="role-management">
       <el-button type="primary" @click="handleCreateRole">新建角色</el-button>
       
       <el-table :data="roleList" style="width: 100%; margin-top: 20px;">
         <el-table-column prop="name" label="角色名称" />
         <el-table-column prop="code" label="角色编码" />
         <el-table-column prop="description" label="描述" />
         <el-table-column prop="status" label="状态">
           <template #default="scope">
             <el-tag :type="scope.row.status === 1 ? 'success' : 'danger'">
               {{ scope.row.status === 1 ? '正常' : '禁用' }}
             </el-tag>
           </template>
         </el-table-column>
         <el-table-column label="操作" width="200">
           <template #default="scope">
             <el-button size="small" @click="handleEditRole(scope.row)">编辑</el-button>
             <el-button size="small" @click="handleAssignPermission(scope.row)">分配权限</el-button>
           </template>
         </el-table-column>
       </el-table>
       
       <!-- 权限分配对话框 -->
       <el-dialog v-model="permissionDialogVisible" title="分配权限" width="600px">
         <el-tree
           ref="permissionTreeRef"
           :data="permissionTree"
           show-checkbox
           node-key="id"
           :props="treeProps"
           :default-checked-keys="checkedPermissions"
         />
         <template #footer>
           <el-button @click="permissionDialogVisible = false">取消</el-button>
           <el-button type="primary" @click="handleSavePermissions">保存</el-button>
         </template>
       </el-dialog>
     </div>
   </template>
   ```

#### 周六（4月16日）：用户模块集成测试
1. **前后端联调测试**
   ```bash
   # 启动完整系统
   cd backend && mvn spring-boot:run
   cd frontend && npm run dev
   
   # 测试用户管理功能
   # 1. 登录系统
   # 2. 访问用户管理页面
   # 3. 测试用户搜索、筛选
   # 4. 测试用户创建、编辑、删除
   # 5. 测试用户状态切换
   ```

2. **与自动化测试框架对接验证**
   ```bash
   # 在自动化测试框架项目中
   cd f:\Desk\graduation project\ecommerce-autotest
   
   # 更新测试数据中的用户信息
   # config/testdata/user_permission.yaml
   
   # 运行用户管理测试用例
   python run.py --run user
   
   # 检查测试结果，记录问题
   ```

3. **修复发现的问题**
   - 调整页面元素定位
   - 优化API响应格式
   - 修复权限验证逻辑
   - 改善错误提示信息

#### 周日（4月17日）：第二周验收和文档
1. **功能验收测试**
   - 用户管理完整功能测试
   - 权限管理功能测试
   - 安全认证测试
   - 性能基准测试

2. **兼容性验证**
   - 自动化测试框架用户测试用例通过率
   - 页面元素匹配度检查
   - API接口兼容性验证

3. **文档更新**
   - 更新API接口文档
   - 编写用户管理模块使用指南
   - 记录技术实现细节
   - 更新部署配置

### 第2周交付物
- [x] 用户管理完整功能（前后端）
- [x] 角色和权限管理模块
- [x] Spring Security + JWT认证集成
- [x] 用户管理前端页面（兼容自动化测试）
- [x] 权限分配功能
- [x] 用户模块单元测试
- [x] 第二周验收报告
- [x] 用户管理API文档

### 第3周目标（2026年4月18日-4月24日）
完成商品管理模块开发，确保与自动化测试框架的商品管理测试用例兼容。

### 第3周详细任务

#### 周一（4月18日）：商品管理后端开发
1. **商品实体设计**
   ```java
   // backend/src/main/java/com/shop/dao/entity/Product.java
   @Data
   @TableName("product")
   public class Product {
       @TableId(type = IdType.AUTO)
       private Long id;
       
       private String name;
       private String description;
       private Long categoryId;
       private BigDecimal price;
       private BigDecimal originalPrice;
       private Integer stock;
       private Integer sales;
       private Integer status; // 0-下架, 1-上架
       private Integer isHot; // 0-否, 1-是
       private Integer isNew; // 0-否, 1-是
       private String mainImage;
       private String images; // JSON数组
       
       @TableField(fill = FieldFill.INSERT)
       private LocalDateTime createTime;
       
       @TableField(fill = FieldFill.INSERT_UPDATE)
       private LocalDateTime updateTime;
   }
   ```

2. **商品分类管理**
   ```java
   // backend/src/main/java/com/shop/dao/entity/Category.java
   @Data
   @TableName("category")
   public class Category {
       @TableId(type = IdType.AUTO)
       private Long id;
       
       private String name;
       private String description;
       private Long parentId;
       private Integer level;
       private Integer sortOrder;
       private Integer status;
       
       @TableField(fill = FieldFill.INSERT)
       private LocalDateTime createTime;
   }
   ```

#### 周二（4月19日）：商品管理API设计
1. **商品CRUD API**
   ```java
   // backend/src/main/java/com/shop/controller/admin/AdminProductController.java
   @RestController
   @RequestMapping("/api/admin/products")
   public class AdminProductController {
       
       @GetMapping("")
       public ApiResponse<?> listProducts(
           @RequestParam(defaultValue = "1") Integer page,
           @RequestParam(defaultValue = "10") Integer size,
           @RequestParam(required = false) String name,
           @RequestParam(required = false) Long categoryId,
           @RequestParam(required = false) Integer status,
           @RequestParam(required = false) Integer isHot,
           @RequestParam(required = false) Integer isNew
       ) {
           // 商品列表（支持搜索筛选）
       }
       
       @GetMapping("/{id}")
       public ApiResponse<?> getProductDetail(@PathVariable Long id) {
           // 商品详情
       }
       
       @PostMapping("")
       public ApiResponse<?> createProduct(@Valid @RequestBody ProductCreateRequest request) {
           // 创建商品
       }
       
       @PutMapping("/{id}")
       public ApiResponse<?> updateProduct(@PathVariable Long id, @Valid @RequestBody ProductUpdateRequest request) {
           // 更新商品
       }
       
       @DeleteMapping("/{id}")
       public ApiResponse<?> deleteProduct(@PathVariable Long id) {
           // 删除商品（软删除）
       }
       
       @PostMapping("/upload-image")
       public ApiResponse<?> uploadImage(@RequestParam("file") MultipartFile file) {
           // 上传商品图片
       }
       
       @PatchMapping("/{id}/status")
       public ApiResponse<?> updateProductStatus(@PathVariable Long id, @RequestParam Integer status) {
           // 更新商品状态
       }
   }
   ```

#### 周三（4月20日）：商品管理前端开发（兼容性关键）
1. **商品列表页面（必须严格匹配自动化测试框架）**
   ```vue
   <!-- frontend/src/views/admin/Product/List.vue -->
   <template>
     <div class="product-management">
       <div class="search-bar">
         <el-input
           v-model="searchParams.name"
           placeholder="搜索商品"
           id="product-search"  <!-- 必须：自动化测试定位器 -->
           style="width: 200px; margin-right: 10px;"
           @keyup.enter="handleSearch"
         />
         <el-select
           v-model="searchParams.categoryId"
           placeholder="分类"
           clearable
           id="category-filter"  <!-- 必须：自动化测试定位器 -->
           style="width: 150px; margin-right: 10px;"
         >
           <el-option
             v-for="category in categoryList"
             :key="category.id"
             :label="category.name"
             :value="category.id"
           />
         </el-select>
         <el-select
           v-model="searchParams.status"
           placeholder="状态"
           clearable
           style="width: 100px; margin-right: 10px;"
         >
           <el-option label="上架" :value="1" />
           <el-option label="下架" :value="0" />
         </el-select>
         <el-button type="primary" @click="handleSearch">搜索</el-button>
         <el-button @click="handleReset">重置</el-button>
         <el-button type="success" @click="handleAdd" id="add-product-btn">  <!-- 必须：自动化测试定位器 -->
           添加商品
         </el-button>
       </div>
       
       <el-table
         :data="productList"
         v-loading="loading"
         id="product-table"  <!-- 必须：自动化测试定位器 -->
         style="width: 100%; margin-top: 20px;"
       >
         <el-table-column prop="id" label="ID" width="80" />
         <el-table-column prop="name" label="商品名称" min-width="200" />
         <el-table-column prop="categoryName" label="分类" width="120" />
         <el-table-column prop="price" label="价格" width="100">
           <template #default="scope">
             ¥{{ scope.row.price }}
           </template>
         </el-table-column>
         <el-table-column prop="stock" label="库存" width="80" />
         <el-table-column prop="sales" label="销量" width="80" />
         <el-table-column prop="status" label="状态" width="80">
           <template #default="scope">
             <el-tag :type="scope.row.status === 1 ? 'success' : 'info'">
               {{ scope.row.status === 1 ? '上架' : '下架' }}
             </el-tag>
           </template>
         </el-table-column>
         <el-table-column prop="createTime" label="创建时间" width="180" />
         <el-table-column label="操作" width="200">
           <template #default="scope">
             <el-button size="small" @click="handleEdit(scope.row)">编辑</el-button>
             <el-button size="small" @click="handleToggleStatus(scope.row)">
               {{ scope.row.status === 1 ? '下架' : '上架' }}
             </el-button>
             <el-button size="small" type="danger" @click="handleDelete(scope.row)">删除</el-button>
           </template>
         </el-table-column>
       </el-table>
       
       <el-pagination
         v-model:current-page="pagination.current"
         v-model:page-size="pagination.size"
         :total="pagination.total"
         @current-change="handlePageChange"
         @size-change="handleSizeChange"
         style="margin-top: 20px; text-align: right;"
       />
     </div>
   </template>
   ```

#### 周四（4月21日）：商品图片上传和富文本编辑器
1. **图片上传组件**
   ```vue
   <!-- frontend/src/components/common/ImageUpload.vue -->
   <template>
     <div class="image-upload">
       <el-upload
         class="upload-demo"
         :action="uploadUrl"
         :headers="headers"
         :on-success="handleSuccess"
         :on-error="handleError"
         :before-upload="beforeUpload"
         :show-file-list="false"
       >
         <el-button type="primary">点击上传</el-button>
         <template #tip>
           <div class="el-upload__tip">
             支持jpg/png格式，大小不超过2MB
           </div>
         </template>
       </el-upload>
       
       <div v-if="imageUrl" class="image-preview">
         <img :src="imageUrl" alt="预览" />
         <el-button
           type="danger"
           size="small"
           @click="handleRemove"
           class="remove-btn"
         >
           删除
         </el-button>
       </div>
     </div>
   </template>
   ```

2. **商品编辑页面（兼容性关键）**
   ```vue
   <!-- frontend/src/views/admin/Product/Edit.vue -->
   <template>
     <div class="product-edit">
       <el-form
         ref="formRef"
         :model="form"
         :rules="rules"
         label-width="100px"
         style="max-width: 800px;"
       >
         <el-form-item label="商品名称" prop="name">
           <el-input
             v-model="form.name"
             placeholder="请输入商品名称"
             id="product-name"  <!-- 必须：自动化测试定位器 -->
           />
         </el-form-item>
         
         <el-form-item label="商品分类" prop="categoryId">
           <el-select
             v-model="form.categoryId"
             placeholder="请选择分类"
             style="width: 100%;"
           >
             <el-option
               v-for="category in categoryList"
               :key="category.id"
               :label="category.name"
               :value="category.id"
             />
           </el-select>
         </el-form-item>
         
         <el-form-item label="商品价格" prop="price">
           <el-input-number
             v-model="form.price"
             :min="0"
             :step="0.01"
             placeholder="请输入价格"
             id="product-price"  <!-- 必须：自动化测试定位器 -->
           />
         </el-form-item>
         
         <el-form-item label="商品库存" prop="stock">
           <el-input-number
             v-model="form.stock"
             :min="0"
             placeholder="请输入库存"
             id="product-stock"  <!-- 必须：自动化测试定位器 -->
           />
         </el-form-item>
         
         <el-form-item label="商品状态" prop="status">
           <el-select
             v-model="form.status"
             placeholder="请选择状态"
             id="product-status"  <!-- 必须：自动化测试定位器 -->
           >
             <el-option label="上架" :value="1" />
             <el-option label="下架" :value="0" />
           </el-select>
         </el-form-item>
         
         <el-form-item label="商品图片">
           <ImageUpload v-model="form.mainImage" />
         </el-form-item>
         
         <el-form-item label="商品描述">
           <el-input
             v-model="form.description"
             type="textarea"
             :rows="4"
             placeholder="请输入商品描述"
           />
         </el-form-item>
         
         <el-form-item>
           <el-button
             type="primary"
             @click="handleSubmit"
             id="save-product-btn"  <!-- 必须：自动化测试定位器 -->
           >
             保存
           </el-button>
           <el-button
             @click="handleCancel"
             id="cancel-btn"  <!-- 必须：自动化测试定位器 -->
           >
             取消
           </el-button>
         </el-form-item>
       </el-form>
     </div>
   </template>
   ```

#### 周五（4月22日）：商品搜索和筛选优化
1. **商品搜索服务优化**
   ```java
   // backend/src/main/java/com/shop/service/impl/ProductServiceImpl.java
   @Service
   public class ProductServiceImpl implements ProductService {
       
       @Override
       public Page<Product> searchProducts(ProductQuery query) {
           Page<Product> page = new Page<>(query.getPage(), query.getSize());
           LambdaQueryWrapper<Product> wrapper = new LambdaQueryWrapper<>();
           
           // 关键词搜索（名称、描述）
           if (StringUtils.isNotBlank(query.getKeyword())) {
               wrapper.and(w -> w
                   .like(Product::getName, query.getKeyword())
                   .or()
                   .like(Product::getDescription, query.getKeyword())
               );
           }
           
           // 分类筛选
           if (query.getCategoryId() != null) {
               wrapper.eq(Product::getCategoryId, query.getCategoryId());
           }
           
           // 状态筛选
           if (query.getStatus() != null) {
               wrapper.eq(Product::getStatus, query.getStatus());
           }
           
           // 价格范围筛选
           if (query.getMinPrice() != null) {
               wrapper.ge(Product::getPrice, query.getMinPrice());
           }
           if (query.getMaxPrice() != null) {
               wrapper.le(Product::getPrice, query.getMaxPrice());
           }
           
           // 排序
           if (StringUtils.isNotBlank(query.getSortBy())) {
               if ("price_asc".equals(query.getSortBy())) {
                   wrapper.orderByAsc(Product::getPrice);
               } else if ("price_desc".equals(query.getSortBy())) {
                   wrapper.orderByDesc(Product::getPrice);
               } else if ("sales_desc".equals(query.getSortBy())) {
                   wrapper.orderByDesc(Product::getSales);
               } else {
                   wrapper.orderByDesc(Product::getCreateTime);
               }
           } else {
               wrapper.orderByDesc(Product::getCreateTime);
           }
           
           return productMapper.selectPage(page, wrapper);
       }
   }
   ```

#### 周六（4月23日）：商品模块集成测试
1. **前后端联调测试**
   ```bash
   # 启动系统
   cd backend && mvn spring-boot:run
   cd frontend && npm run dev
   
   # 测试商品管理功能
   # 1. 登录系统
   # 2. 访问商品管理页面
   # 3. 测试商品搜索、筛选
   # 4. 测试商品添加、编辑、删除
   # 5. 测试商品图片上传
   # 6. 测试商品状态切换
   ```

2. **与自动化测试框架对接验证**
   ```bash
   # 在自动化测试框架项目中
   cd f:\Desk\graduation project\ecommerce-autotest
   
   # 更新测试数据中的商品信息
   # config/testdata/product.yaml
   
   # 运行商品管理测试用例
   python run.py --run product
   
   # 检查测试结果，记录问题
   ```

3. **性能测试**
   - 商品列表加载性能测试
   - 商品搜索响应时间测试
   - 图片上传性能测试
   - 数据库查询优化

#### 周日（4月24日）：第三周验收和文档
1. **功能验收测试**
   - 商品管理完整功能测试
   - 商品分类管理测试
   - 图片上传功能测试
   - 搜索筛选功能测试

2. **兼容性验证**
   - 自动化测试框架商品测试用例通过率
   - 页面元素匹配度检查
   - 表单验证兼容性

3. **文档更新**
   - 更新商品管理API文档
   - 编写商品管理使用指南
   - 记录图片上传配置
   - 更新部署配置

### 第3周交付物
- [x] 商品管理完整功能（前后端）
- [x] 商品分类管理模块
- [x] 商品图片上传功能
- [x] 商品搜索筛选功能
- [x] 商品管理前端页面（兼容自动化测试）
- [x] 商品模块单元测试
- [x] 第三周验收报告
- [x] 商品管理API文档

### 第4周目标（2026年4月25日-5月1日）
完成订单管理和前台商城模块，确保与自动化测试框架的订单管理测试用例兼容。

### 第4周详细任务

#### 周一（4月25日）：订单管理后端开发
1. **订单实体设计**
   ```java
   // backend/src/main/java/com/shop/dao/entity/Order.java
   @Data
   @TableName("`order`")
   public class Order {
       @TableId(type = IdType.AUTO)
       private Long id;
       
       private String orderNo;
       private Long userId;
       private BigDecimal totalAmount;
       private BigDecimal discountAmount;
       private BigDecimal shippingFee;
       private BigDecimal actualAmount;
       private Integer status; // 1-待付款, 2-待发货, 3-已发货, 4-已完成, 5-已取消
       private Integer paymentMethod; // 1-支付宝, 2-微信, 3-银行卡
       private LocalDateTime paymentTime;
       private LocalDateTime shippingTime;
       private LocalDateTime receiveTime;
       private LocalDateTime cancelTime;
       private String cancelReason;
       private String shippingAddress; // JSON格式
       private String remark;
       
       @TableField(fill = FieldFill.INSERT)
       private LocalDateTime createTime;
       
       @TableField(fill = FieldFill.INSERT_UPDATE)
       private LocalDateTime updateTime;
   }
   ```

2. **订单商品关系表**
   ```java
   // backend/src/main/java/com/shop/dao/entity/OrderItem.java
   @Data
   @TableName("order_item")
   public class OrderItem {
       @TableId(type = IdType.AUTO)
       private Long id;
       
       private Long orderId;
       private Long productId;
       private String productName;
       private String productImage;
       private BigDecimal price;
       private Integer quantity;
       private BigDecimal totalPrice;
   }
   ```

#### 周二（4月26日）：订单管理API设计
1. **订单管理API**
   ```java
   // backend/src/main/java/com/shop/controller/admin/AdminOrderController.java
   @RestController
   @RequestMapping("/api/admin/orders")
   public class AdminOrderController {
       
       @GetMapping("")
       public ApiResponse<?> listOrders(
           @RequestParam(defaultValue = "1") Integer page,
           @RequestParam(defaultValue = "10") Integer size,
           @RequestParam(required = false) String orderNo,
           @RequestParam(required = false) String username,
           @RequestParam(required = false) Integer status,
           @RequestParam(required = false) @DateTimeFormat(pattern = "yyyy-MM-dd") LocalDate startDate,
           @RequestParam(required = false) @DateTimeFormat(pattern = "yyyy-MM-dd") LocalDate endDate
       ) {
           // 订单列表（支持复杂搜索）
       }
       
       @GetMapping("/{id}")
       public ApiResponse<?> getOrderDetail(@PathVariable Long id) {
           // 订单详情
       }
       
       @PatchMapping("/{id}/status")
       public ApiResponse<?> updateOrderStatus(
           @PathVariable Long id,
           @RequestParam Integer status,
           @RequestParam(required = false) String remark
       ) {
           // 更新订单状态
       }
       
       @PostMapping("/{id}/ship")
       public ApiResponse<?> shipOrder(
           @PathVariable Long id,
           @RequestBody OrderShipRequest request
       ) {
           // 发货操作
       }
       
       @GetMapping("/export")
       public void exportOrders(
           HttpServletResponse response,
           @RequestParam(required = false) String orderNo,
           @RequestParam(required = false) Integer status,
           @RequestParam(required = false) @DateTimeFormat(pattern = "yyyy-MM-dd") LocalDate startDate,
           @RequestParam(required = false) @DateTimeFormat(pattern = "yyyy-MM-dd") LocalDate endDate
       ) {
           // 导出订单Excel
       }
       
       @GetMapping("/statistics")
       public ApiResponse<?> getOrderStatistics(
           @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") LocalDate startDate,
           @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") LocalDate endDate
       ) {
           // 订单统计
       }
   }
   ```

#### 周三（4月27日）：订单管理前端开发（兼容性关键）
1. **订单列表页面（必须严格匹配自动化测试框架）**
   ```vue
   <!-- frontend/src/views/admin/Order/List.vue -->
   <template>
     <div class="order-management">
       <div class="search-bar">
         <el-input
           v-model="searchParams.orderNo"
           placeholder="订单号"
           id="order-search-input"  <!-- 必须：自动化测试定位器 -->
           style="width: 200px; margin-right: 10px;"
         />
         <el-select
           v-model="searchParams.status"
           placeholder="订单状态"
           clearable
           id="status-filter"  <!-- 必须：自动化测试定位器 -->
           style="width: 120px; margin-right: 10px;"
         >
           <el-option label="待付款" :value="1" />
           <el-option label="待发货" :value="2" />
           <el-option label="已发货" :value="3" />
           <el-option label="已完成" :value="4" />
           <el-option label="已取消" :value="5" />
         </el-select>
         <el-date-picker
           v-model="searchParams.dateRange"
           type="daterange"
           range-separator="至"
           start-placeholder="开始日期"
           end-placeholder="结束日期"
           style="width: 300px; margin-right: 10px;"
         />
         <el-button type="primary" @click="handleSearch">搜索</el-button>
         <el-button @click="handleReset">重置</el-button>
         <el-button @click="handleExport">导出</el-button>
       </div>
       
       <el-table
         :data="orderList"
         v-loading="loading"
         id="order-table"  <!-- 必须：自动化测试定位器 -->
         style="width: 100%; margin-top: 20px;"
       >
         <el-table-column prop="orderNo" label="订单号" width="180" />
         <el-table-column prop="username" label="用户" width="120" />
         <el-table-column prop="totalAmount" label="总金额" width="100">
           <template #default="scope">
             ¥{{ scope.row.totalAmount }}
           </template>
         </el-table-column>
         <el-table-column prop="actualAmount" label="实付金额" width="100">
           <template #default="scope">
             ¥{{ scope.row.actualAmount }}
           </template>
         </el-table-column>
         <el-table-column prop="status" label="状态" width="100">
           <template #default="scope">
             <el-tag :type="getStatusTagType(scope.row.status)">
               {{ getStatusText(scope.row.status) }}
             </el-tag>
           </template>
         </el-table-column>
         <el-table-column prop="createTime" label="创建时间" width="180" />
         <el-table-column label="操作" width="200">
           <template #default="scope">
             <el-button size="small" @click="handleViewDetail(scope.row)">详情</el-button>
             <el-button
               v-if="scope.row.status === 2"
               size="small"
               type="success"
               @click="handleShip(scope.row)"
             >
               发货
             </el-button>
           </template>
         </el-table-column>
       </el-table>
       
       <el-pagination
         v-model:current-page="pagination.current"
         v-model:page-size="pagination.size"
         :total="pagination.total"
         @current-change="handlePageChange"
         @size-change="handleSizeChange"
         style="margin-top: 20px; text-align: right;"
       />
     </div>
   </template>
   ```

#### 周四（4月28日）：前台商城基础功能
1. **商品展示页面**
   ```vue
   <!-- frontend/src/views/mall/ProductList.vue -->
   <template>
     <div class="product-list-page">
       <div class="filter-bar">
         <el-select v-model="filterParams.categoryId" placeholder="全部分类" clearable>
           <el-option
             v-for="category in categoryList"
             :key="category.id"
             :label="category.name"
             :value="category.id"
           />
         </el-select>
         
         <el-select v-model="filterParams.sortBy" placeholder="排序方式" style="margin-left: 10px;">
           <el-option label="默认排序" value="default" />
           <el-option label="价格从低到高" value="price_asc" />
           <el-option label="价格从高到低" value="price_desc" />
           <el-option label="销量从高到低" value="sales_desc" />
         </el-select>
         
         <el-input-number
           v-model="filterParams.minPrice"
           placeholder="最低价"
           :min="0"
           style="margin-left: 10px; width: 120px;"
         />
         <span style="margin: 0 5px;">-</span>
         <el-input-number
           v-model="filterParams.maxPrice"
           placeholder="最高价"
           :min="0"
           style="width: 120px;"
         />
       </div>
       
       <div class="product-grid">
         <div
           v-for="product in productList"
           :key="product.id"
           class="product-card"
         >
           <img :src="product.mainImage" :alt="product.name" class="product-image" />
           <div class="product-info">
             <h3 class="product-name">{{ product.name }}</h3>
             <p class="product-price">¥{{ product.price }}</p>
             <p class="product-sales">已售 {{ product.sales }}</p>
             <el-button type="primary" size="small" @click="handleAddToCart(product)">
               加入购物车
             </el-button>
           </div>
         </div>
       </div>
     </div>
   </template>
   ```

2. **购物车功能**
   ```vue
   <!-- frontend/src/views/mall/Cart.vue -->
   <template>
     <div class="cart-page">
       <el-table :data="cartItems" style="width: 100%;">
         <el-table-column label="商品" width="300">
           <template #default="scope">
             <div class="cart-item">
               <img :src="scope.row.productImage" :alt="scope.row.productName" />
               <span>{{ scope.row.productName }}</span>
             </div>
           </template>
         </el-table-column>
         <el-table-column prop="price" label="单价" width="100">
           <template #default="scope">¥{{ scope.row.price }}</template>
         </el-table-column>
         <el-table-column label="数量" width="150">
           <template #default="scope">
             <el-input-number
               v-model="scope.row.quantity"
               :min="1"
               :max="scope.row.stock"
               @change="handleQuantityChange(scope.row)"
             />
           </template>
         </el-table-column>
         <el-table-column label="小计" width="100">
           <template #default="scope">
             ¥{{ scope.row.price * scope.row.quantity }}
           </template>
         </el-table-column>
         <el-table-column label="操作" width="100">
           <template #default="scope">
             <el-button type="danger" size="small" @click="handleRemove(scope.row)">
               删除
             </el-button>
           </template>
         </el-table-column>
       </el-table>
       
       <div class="cart-summary">
         <div class="total-amount">
           总计: ¥{{ totalAmount }}
         </div>
         <el-button type="primary" size="large" @click="handleCheckout">
           去结算
         </el-button>
       </div>
     </div>
   </template>
   ```

#### 周五（4月29日）：订单流程和状态管理
1. **订单状态流转逻辑**
   ```java
   // backend/src/main/java/com/shop/service/impl/OrderServiceImpl.java
   @Service
   public class OrderServiceImpl implements OrderService {
       
       @Override
       @Transactional
       public void updateOrderStatus(Long orderId, Integer newStatus, String remark) {
           Order order = orderMapper.selectById(orderId);
           if (order == null) {
               throw new BusinessException("订单不存在");
           }
           
           // 状态流转验证
           validateStatusTransition(order.getStatus(), newStatus);
           
           // 更新订单状态
           order.setStatus(newStatus);
           order.setRemark(remark);
           
           // 记录状态变更时间
           switch (newStatus) {
               case 2: // 已付款
                   order.setPaymentTime(LocalDateTime.now());
                   break;
               case 3: // 已发货
                   order.setShippingTime(LocalDateTime.now());
                   break;
               case 4: // 已完成
                   order.setReceiveTime(LocalDateTime.now());
                   break;
               case 5: // 已取消
                   order.setCancelTime(LocalDateTime.now());
                   break;
           }
           
           orderMapper.updateById(order);
           
           // 记录订单操作日志
           saveOrderLog(orderId, order.getStatus(), newStatus, remark);
       }
       
       private void validateStatusTransition(Integer currentStatus, Integer newStatus) {
           // 状态流转规则验证
           if (currentStatus.equals(newStatus)) {
               throw new BusinessException("订单状态未变化");
           }
           
           // 状态流转图验证
           Map<Integer, List<Integer>> allowedTransitions = Map.of(
               1, List.of(2, 5),  // 待付款 → 待发货/已取消
               2, List.of(3, 5),  // 待发货 → 已发货/已取消
               3, List.of(4),     // 已发货 → 已完成
               4, List.of(),      // 已完成 → 无
               5, List.of()       // 已取消 → 无
           );
           
           if (!allowedTransitions.getOrDefault(currentStatus, List.of()).contains(newStatus)) {
               throw new BusinessException("状态流转不允许");
           }
       }
   }
   ```

#### 周六（4月30日）：订单模块集成测试
1. **前后端联调测试**
   ```bash
   # 启动系统
   cd backend && mvn spring-boot:run
   cd frontend && npm run dev
   
   # 测试订单管理功能
   # 1. 登录后台系统
   # 2. 访问订单管理页面
   # 3. 测试订单搜索、筛选
   # 4. 测试订单状态流转
   # 5. 测试订单导出功能
   
   # 测试前台商城功能
   # 1. 访问前台商城
   # 2. 测试商品浏览
   # 3. 测试购物车功能
   # 4. 测试订单创建流程
   ```

2. **与自动化测试框架对接验证**
   ```bash
   # 在自动化测试框架项目中
   cd f:\Desk\graduation project\ecommerce-autotest
   
   # 更新测试数据中的订单信息
   # config/testdata/order.yaml
   
   # 运行订单管理测试用例
   python run.py --run order
   
   # 检查测试结果，记录问题
   ```

3. **端到端流程测试**
   - 前台用户注册 → 商品浏览 → 加入购物车 → 创建订单
   - 后台订单处理 → 发货 → 完成订单
   - 用户订单查看 → 确认收货

#### 周日（5月1日）：第四周验收和文档
1. **功能验收测试**
   - 订单管理完整功能测试
   - 订单状态流转测试
   - 前台商城功能测试
   - 购物车功能测试
   - 订单导出功能测试

2. **兼容性验证**
   - 自动化测试框架订单测试用例通过率
   - 页面元素匹配度检查
   - 订单流程兼容性验证

3. **文档更新**
   - 更新订单管理API文档
   - 编写订单管理使用指南
   - 记录前台商城配置
   - 更新部署配置

### 第4周交付物
- [x] 订单管理完整功能（前后端）
- [x] 订单状态流转逻辑
- [x] 前台商城基础功能
- [x] 购物车功能
- [x] 订单导出功能
- [x] 订单管理前端页面（兼容自动化测试）
- [x] 订单模块单元测试
- [x] 第四周验收报告
- [x] 订单管理API文档

## 第三阶段：系统集成与优化（第5周：2026年5月2日-5月8日）

### 第5周目标
完成系统集成，进行性能优化、安全加固和兼容性测试，确保自动化测试框架完整测试套件通过。

### 第5周详细任务

#### 周一（5月2日）：前后端完整联调
1. **跨模块功能测试**
   - 用户登录 → 权限验证 → 商品管理 → 订单处理完整流程
   - 前台商城购物 → 后台订单处理端到端流程
   - 权限分配 → 菜单访问验证

2. **API接口联调**
   - 验证所有API接口正常工作
   - 测试异常情况处理
   - 验证数据一致性

3. **数据库性能优化**
   ```sql
   -- 创建索引优化查询性能
   CREATE INDEX idx_product_status ON product(status);
   CREATE INDEX idx_product_category ON product(category_id);
   CREATE INDEX idx_order_status ON `order`(status);
   CREATE INDEX idx_order_create_time ON `order`(create_time);
   CREATE INDEX idx_order_user ON `order`(user_id);
   
   -- 优化查询语句
   EXPLAIN SELECT * FROM product WHERE status = 1 AND category_id = 1;
   ```

#### 周二（5月3日）：性能测试与优化
1. **性能基准测试**
   ```bash
   # 使用JMeter进行性能测试
   # 测试场景：
   # 1. 用户登录压力测试
   # 2. 商品列表加载性能
   # 3. 订单搜索响应时间
   # 4. 图片上传并发测试
   ```

2. **前端性能优化**
   ```javascript
   // frontend/vite.config.ts - 构建优化
   export default defineConfig({
     build: {
       rollupOptions: {
         output: {
           manualChunks: {
             vue: ['vue', 'vue-router', 'pinia'],
             element: ['element-plus'],
             vendor: ['axios', 'lodash']
           }
         }
       },
       chunkSizeWarningLimit: 1000
     }
   })
   ```

3. **后端性能优化**
   ```java
   // 使用缓存优化热点数据
   @Cacheable(value = "products", key = "#id")
   public Product getProductById(Long id) {
       return productMapper.selectById(id);
   }
   
   // 分页查询优化
   public Page<Product> listProducts(ProductQuery query) {
       Page<Product> page = new Page<>(query.getPage(), query.getSize());
       page.setSearchCount(true); // 避免count查询
       return productMapper.selectPage(page, wrapper);
   }
   ```

#### 周三（5月4日）：安全测试与加固
1. **安全漏洞扫描**
   ```bash
   # 使用OWASP Dependency-Check检查依赖漏洞
   mvn org.owasp:dependency-check-maven:check
   
   # 使用SonarQube进行代码安全扫描
   ```

2. **安全配置加固**
   ```yaml
   # application-security.yml
   security:
     jwt:
       secret: ${JWT_SECRET}  # 从环境变量读取
       expiration: 3600000    # 1小时
       refresh-expiration: 604800000  # 7天
     
     cors:
       allowed-origins: "${ALLOWED_ORIGINS:http://localhost:3000}"
       allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
       allowed-headers: "Authorization,Content-Type"
       allow-credentials: true
       max-age: 3600
     
     headers:
       content-security-policy: "default-src 'self'"
       x-frame-options: "DENY"
       x-content-type-options: "nosniff"
       x-xss-protection: "1; mode=block"
   ```

3. **输入验证和输出编码**
   ```java
   // 使用Spring Validation进行输入验证
   @PostMapping("")
   public ApiResponse<?> createProduct(@Valid @RequestBody ProductCreateRequest request) {
       // 自动验证@NotNull, @Size, @Min等注解
   }
   
   // 输出编码防止XSS
   @Configuration
   public class WebConfig implements WebMvcConfigurer {
       @Override
       public void configureMessageConverters(List<HttpMessageConverter<?>> converters) {
           StringHttpMessageConverter converter = new StringHttpMessageConverter(StandardCharsets.UTF_8);
           converter.setWriteAcceptCharset(false);
           converters.add(new MappingJackson2HttpMessageConverter());
       }
   }
   ```

#### 周四（5月5日）：兼容性测试验证
1. **自动化测试框架完整测试**
   ```bash
   # 在自动化测试框架项目中运行完整测试套件
   cd f:\Desk\graduation project\ecommerce-autotest
   
   # 运行所有测试用例
   python run.py --run all
   
   # 检查测试报告，记录未通过的用例
   # 分析失败原因，制定修复计划
   ```

2. **浏览器兼容性测试**
   - Chrome 90+ 兼容性测试
   - Firefox 88+ 兼容性测试
   - Edge 90+ 兼容性测试
   - 不同分辨率测试（1920×1080, 1366×768, 1536×864）

3. **移动端适配测试**
   - 响应式布局测试
   - 移动端触摸操作测试
   - 移动端性能测试

#### 周五（5月6日）：Bug修复和优化
1. **根据测试结果修复问题**
   - 修复自动化测试未通过的用例
   - 修复性能测试发现的问题
   - 修复安全测试发现的漏洞
   - 修复浏览器兼容性问题

2. **用户体验优化**
   - 优化页面加载速度
   - 改善错误提示信息
   - 优化表单验证体验
   - 添加加载状态提示

3. **代码质量优化**
   ```bash
   # 运行代码质量检查
   mvn checkstyle:check
   mvn pmd:check
   mvn spotbugs:check
   
   # 运行单元测试
   mvn test
   
   # 生成测试覆盖率报告
   mvn jacoco:report
   ```

#### 周六（5月7日）：系统监控和健康检查
1. **Spring Boot Actuator配置**
   ```yaml
   # application.yml
   management:
     endpoints:
       web:
         exposure:
           include: "health,info,metrics,prometheus"
     endpoint:
       health:
         show-details: always
     metrics:
       export:
         prometheus:
           enabled: true
   ```

2. **健康检查接口**
   ```java
   @Component
   public class CustomHealthIndicator implements HealthIndicator {
       
       @Autowired
       private DataSource dataSource;
       
       @Override
       public Health health() {
           // 检查数据库连接
           try (Connection connection = dataSource.getConnection()) {
               if (connection.isValid(1000)) {
                   return Health.up().withDetail("database", "available").build();
               } else {
                   return Health.down().withDetail("database", "unavailable").build();
               }
           } catch (SQLException e) {
               return Health.down().withDetail("database", e.getMessage()).build();
           }
       }
   }
   ```

3. **日志监控配置**
   ```xml
   <!-- logback-spring.xml -->
   <configuration>
     <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
       <file>logs/application.log</file>
       <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
         <fileNamePattern>logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
         <maxHistory>30</maxHistory>
       </rollingPolicy>
       <encoder>
         <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
       </encoder>
     </appender>
     
     <root level="INFO">
       <appender-ref ref="FILE" />
     </root>
   </configuration>
   ```

#### 周日（5月8日）：第五周验收和文档
1. **系统集成验收测试**
   - 完整业务流程测试
   - 性能验收测试
   - 安全验收测试
   - 兼容性验收测试

2. **自动化测试框架最终验证**
   ```bash
   # 运行完整自动化测试套件
   python run.py --run all --workers 4
   
   # 生成最终测试报告
   # 记录通过率、执行时间等关键指标
   ```

3. **文档整理**
   - 更新系统集成文档
   - 编写性能优化报告
   - 记录安全加固措施
   - 整理兼容性测试报告

### 第5周交付物
- [x] 完整可运行系统
- [x] 性能测试报告和优化方案
- [x] 安全测试报告和加固措施
- [x] 兼容性测试报告
- [x] 自动化测试框架完整测试通过
- [x] 系统监控配置
- [x] 健康检查接口
- [x] 第五周验收报告

## 第四阶段：部署与文档（第6周：2026年5月9日-5月15日）

### 第6周目标
完成多环境部署配置，编写完整项目文档，准备项目交付。

### 第6周详细任务

#### 周一（5月9日）：多环境部署配置
1. **环境配置文件**
   ```yaml
   # application-dev.yml (开发环境)
   spring:
     datasource:
       url: jdbc:mysql://localhost:3306/shop_system_dev
     redis:
       host: localhost
       port: 6379
   
   # application-test.yml (测试环境)
   spring:
     datasource:
       url: jdbc:mysql://test-db:3306/shop_system_test
     redis:
       host: test-redis
       port: 6379
   
   # application-prod.yml (生产环境)
   spring:
     datasource:
       url: jdbc:mysql://prod-db:3306/shop_system_prod?useSSL=true
       hikari:
         connection-timeout: 30000
         maximum-pool-size: 50
     redis:
       host: prod-redis
       port: 6379
       password: ${REDIS_PASSWORD}
   ```

2. **Docker部署配置**
   ```dockerfile
   # backend/Dockerfile
   FROM amazoncorretto:11-alpine
   WORKDIR /app
   COPY target/*.jar app.jar
   EXPOSE 8080
   ENTRYPOINT ["java", "-jar", "app.jar"]
   
   # frontend/Dockerfile
   FROM nginx:alpine
   COPY dist /usr/share/nginx/html
   COPY nginx.conf /etc/nginx/nginx.conf
   EXPOSE 80
   ```

#### 周二（5月10日）：部署脚本编写
1. **Shell部署脚本**
   ```bash
   # deploy.sh
   #!/bin/bash
   
   set -e
   
   echo "开始部署电商后台管理系统..."
   
   # 环境检查
   if ! command -v docker &> /dev/null; then
       echo "错误: Docker未安装"
       exit 1
   fi
   
   if ! command -v docker-compose &> /dev/null; then
       echo "错误: Docker Compose未安装"
       exit 1
   fi
   
   # 创建必要目录
   mkdir -p logs uploads
   
   # 构建镜像
   echo "构建Docker镜像..."
   docker-compose build
   
   # 启动服务
   echo "启动服务..."
   docker-compose up -d
   
   # 等待服务启动
   echo "等待服务启动..."
   sleep 30
   
   # 健康检查
   echo "执行健康检查..."
   curl -f http://localhost:8080/actuator/health || {
       echo "健康检查失败"
       docker-compose logs
       exit 1
   }
   
   echo "部署成功!"
   echo "访问地址: http://localhost"
   echo "后台管理: http://localhost/admin"
   ```

2. **数据库迁移脚本**
   ```sql
   -- database/migrations/V1__init_schema.sql
   -- 初始版本数据库脚本
   
   -- database/migrations/V2__add_indexes.sql
   -- 添加索引优化脚本
   
   -- database/migrations/V3__add_test_data.sql
   -- 测试数据初始化脚本
   ```

#### 周三（5月11日）：系统监控配置
1. **Prometheus监控配置**
   ```yaml
   # prometheus/prometheus.yml
   global:
     scrape_interval: 15s
   
   scrape_configs:
     - job_name: 'spring-boot'
       metrics_path: '/actuator/prometheus'
       static_configs:
         - targets: ['backend:8080']
   
     - job_name: 'mysql'
       static_configs:
         - targets: ['mysql-exporter:9104']
   
     - job_name: 'redis'
       static_configs:
         - targets: ['redis-exporter:9121']
   ```

2. **Grafana仪表板配置**
   ```json
   {
     "dashboard": {
       "title": "电商系统监控",
       "panels": [
         {
           "title": "API请求数",
           "targets": [{"expr": "rate(http_server_requests_seconds_count[5m])"}]
         },
         {
           "title": "数据库连接数",
           "targets": [{"expr": "hikaricp_connections_active"}]
         },
         {
           "title": "JVM内存使用",
           "targets": [{"expr": "jvm_memory_used_bytes"}]
         }
       ]
     }
   }
   ```

#### 周四（5月12日）：完整文档编写
1. **API接口文档**
   ```yaml
   # docs/api/openapi.yaml
   openapi: 3.0.0
   info:
     title: 电商后台管理系统API
     version: 1.0.0
   
   paths:
     /api/auth/login:
       post:
         summary: 用户登录
         requestBody:
           required: true
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/LoginRequest'
         responses:
           '200':
             description: 登录成功
             content:
               application/json:
                 schema:
                   $ref: '#/components/schemas/LoginResponse'
   ```

2. **部署文档**
   ```markdown
   # 部署指南
   
   ## 环境要求
   - Docker 20.10+
   - Docker Compose 2.0+
   - 内存: 4GB+
   - 磁盘: 10GB+
   
   ## 快速开始
   1. 克隆项目
   2. 配置环境变量
   3. 运行部署脚本: ./deploy.sh
   4. 访问系统: http://localhost
   
   ## 配置说明
   - 数据库配置: .env文件
   - 安全配置: application-security.yml
   - 监控配置: prometheus.yml
   ```

#### 周五（5月13日）：用户手册编写
1. **后台管理使用手册**
   ```markdown
   # 后台管理系统使用手册
   
   ## 登录系统
   1. 访问 http://localhost/admin
   2. 输入用户名: admin
   3. 输入密码: admin123
   4. 点击登录按钮
   
   ## 商品管理
   ### 添加商品
   1. 进入商品管理页面
   2. 点击"添加商品"按钮
   3. 填写商品信息
   4. 上传商品图片
   5. 点击"保存"按钮
   
   ### 管理商品
   - 搜索商品: 使用搜索框
   - 筛选商品: 使用分类和状态筛选
   - 编辑商品: 点击编辑按钮
   - 删除商品: 点击删除按钮
   ```

2. **前台商城使用手册**
   ```markdown
   # 前台商城使用手册
   
   ## 注册账号
   1. 点击右上角"注册"按钮
   2. 填写注册信息
   3. 验证邮箱
   4. 登录系统
   
   ## 购物流程
   1. 浏览商品
   2. 加入购物车
   3. 进入购物车
   4. 选择配送地址
   5. 提交订单
   6. 支付订单
   ```

#### 周六（5月14日）：最终验收测试
1. **完整部署测试**
   ```bash
   # 清理环境
   docker-compose down -v
   
   # 全新部署
   ./deploy.sh
   
   # 运行健康检查
   curl http://localhost:8080/actuator/health
   curl http://localhost
   
   # 自动化测试验证
   cd f:\Desk\graduation project\ecommerce-autotest
   python run.py --run all
   ```

2. **性能压测**
   ```bash
   # 使用JMeter进行最终压测
   # 测试并发用户: 100
   # 测试时长: 10分钟
   # 监控指标: 响应时间、错误率、吞吐量
   ```

3. **安全扫描**
   ```bash
   # 最终安全扫描
   mvn org.owasp:dependency-check-maven:check
   # 检查依赖漏洞
   # 生成安全报告
   ```

#### 周日（5月15日）：项目交付准备
1. **交付物整理**
   - 源代码打包
   - 数据库脚本打包
   - 部署脚本打包
   - 文档打包

2. **项目总结报告**
   ```markdown
   # 项目总结报告
   
   ## 项目完成情况
   - 功能完成度: 100%
   - 自动化测试通过率: 100%
   - 性能指标达标: 是
   - 安全测试通过: 是
   
   ## 技术亮点
   1. 前后端分离架构
   2. 与自动化测试框架完美兼容
   3. 完整的安全防护
   4. 多环境部署支持
   
   ## 遇到的问题和解决方案
   1. 页面元素兼容性问题
   2. 性能优化挑战
   3. 安全配置复杂性
   
   ## 后续改进建议
   1. 微服务化改造
   2. 移动端开发
   3. 大数据分析集成
   ```

3. **演示环境准备**
   - 配置演示环境
   - 准备演示数据
   - 测试演示流程
   - 准备演示脚本

### 第6周交付物
- [x] 多环境部署配置
- [x] Docker部署脚本
- [x] 系统监控配置
- [x] 完整API文档
- [x] 部署指南
- [x] 用户手册
- [x] 项目总结报告
- [x] 演示环境
- [x] 最终交付包

## 项目里程碑总结

### 关键里程碑
1. **M1（第1周末）**：基础框架完成，登录页面可访问，兼容性验证通过
2. **M2（第2周末）**：用户认证完成，用户管理测试用例通过
3. **M3（第3周末）**：商品管理完成，商品测试用例通过
4. **M4（第4周末）**：订单管理完成，订单测试用例通过
5. **M5（第5周末）**：系统集成完成，完整测试套件通过
6. **M6（第6周末）**：项目交付完成，文档齐全

### 交付物清单
1. **代码交付物**
   - 完整的Spring Boot后端代码
   - 完整的Vue 3前端代码
   - 数据库SQL脚本
   - 部署脚本和配置

2. **文档交付物**
   - 需求文档
   - 架构设计文档
   - API接口文档
   - 部署运维文档
   - 用户使用手册
   - 项目总结报告

3. **测试交付物**
   - 自动化测试兼容性报告
   - 性能测试报告
   - 安全测试报告
   - 兼容性测试报告

4. **部署交付物**
   - Docker镜像
   - 部署脚本
   - 监控配置
   - 演示环境

## 项目风险管理

### 技术风险应对
| 风险项 | 概率 | 影响 | 应对策略 |
|--------|------|------|----------|
| 前后端集成问题 | 中 | 高 | 每日集成测试，API契约先行 |
| 性能不达标 | 中 | 中 | 性能测试早期进行，持续优化 |
| 安全漏洞 | 低 | 高 | 安全编码规范，定期扫描 |
| 浏览器兼容性问题 | 低 | 中 | 多浏览器测试，使用标准API |

### 项目风险应对
| 风险项 | 概率 | 影响 | 应对策略 |
|--------|------|------|----------|
| 时间不足 | 中 | 高 | 优先级功能先实现，增量开发 |
| 需求理解偏差 | 低 | 中 | 定期沟通确认，原型验证 |
| 技术难点 | 低 | 中 | 技术预研，备选方案 |
| 测试兼容性问题 | 中 | 高 | 每日兼容性验证，及时调整 |

### 与自动化测试框架的兼容性风险
| 风险项 | 概率 | 影响 | 应对策略 |
|--------|------|------|----------|
| 页面元素变更 | 中 | 高 | 元素ID冻结，变更需评审 |
| 业务流程变更 | 低 | 高 | 业务流程文档化，变更同步 |
| 性能不达标 | 低 | 中 | 性能基准测试，持续优化 |
| 测试数据不一致 | 低 | 中 | 统一测试数据管理 |

## 后续维护与扩展

### 维护计划
1. **bug修复**：项目交付后1个月内的bug免费修复
2. **安全更新**：定期安全漏洞扫描和修复
3. **性能优化**：根据监控数据进行性能优化
4. **文档更新**：随系统变更同步更新文档

### 扩展规划
1. **微服务化**：支持拆分为独立的微服务
2. **移动端支持**：开发移动端APP
3. **第三方集成**：支付、物流、客服等第三方服务集成
4. **大数据分析**：用户行为分析、销售预测
5. **国际化**：多语言、多货币支持

### 与自动化测试框架的持续集成
1. **CI/CD集成**：自动化测试集成到部署流水线
2. **兼容性保障**：系统变更前进行自动化测试验证
3. **测试数据同步**：测试数据与系统数据同步更新
4. **性能监控集成**：自动化测试性能数据监控

## 总结

### 项目特点
1. **针对性设计**：专门为自动化测试框架定制的电商系统
2. **高兼容性**：页面元素与测试框架严格匹配
3. **现代化技术栈**：采用Spring Boot + Vue 3等主流技术
4. **完整生态**：包含前后端、数据库、部署运维的全套方案
5. **易于扩展**：支持未来功能扩展和技术演进

### 成功标准
1. **功能完整性**：五大核心模块功能完整实现
2. **测试兼容性**：自动化测试框架全部测试用例通过
3. **性能达标**：满足性能指标要求
4. **安全可靠**：通过基本安全测试
5. **部署顺利**：支持多环境顺利部署

### 与自动化测试项目的协同
本项目与自动化测试框架项目形成完整的毕业设计解决方案：
- **测试框架**：提供自动化测试能力
- **电商系统**：提供测试验证目标
- **协同验证**：相互验证，确保项目质量
- **完整展示**：展示完整的软件开发和质量保障流程

---

*文档版本：1.0*
*创建日期：2026年4月4日*
*项目负责人：宋佳伟*
*指导教师：张刘飘 讲师*
*开发周期：2026年4月4日 - 2026年5月15日*
*备注：本计划专为配合自动化测试框架开发，确保开发进度与测试需求完全匹配*